---
- name: Install Nginx
  hosts: web
  become: yes

  tasks:

    - name: Install Nginx
      apt:
        name: nginx
        state: present
        update_cache: yes

- name: Start Nginx Service
  hosts: web
  become: yes

  tasks:

    - name: Start Service
      service:
        name: nginx
        state: started